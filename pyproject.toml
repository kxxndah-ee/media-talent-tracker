[project]
name = "media-talent-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "altair>=6.3.0",
    "beautifulsoup4>=4.15.0",
    "google-genai>=2.25.0",
    "pandas>=3.0.6",
    "requests>=2.34.2",
    "streamlit>=1.64.0",
]

[project.scripts]
media-talent-tracker = "media_talent_tracker:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
